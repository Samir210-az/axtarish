# Firebase service account (yalnız server tərəfi). İki formatdan biri olur:
# 1) açar faylının bütün JSON mətni
# 2) həmin faylın base64 forması: base64 -w0 service-account.json
FIREBASE_SERVICE_ACCOUNT=
