rules_version = '2';

// Brauzer bazaya birbaşa çıxış etmir. Bütün oxuma və yazma
// serverdən Admin SDK ilə gedir və bu qaydalara tabe olmur.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
